# Chapter 04 TODO

- Explain the new `GetTokenName()` helper that maps token IDs to readable names (used by `EmitTokenStream()`).
- Fix the `BuildEmit` option value in the docs: code uses `--emit=ir` but the chapter text shows `--emit=llvm`.
