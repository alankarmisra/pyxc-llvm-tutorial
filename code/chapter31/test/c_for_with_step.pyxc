# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '0 3 6 9' %t
# RUN: ! grep -q "Error (Line:" %t

def main() -> i32:
    for i in range(0, 10, 3):
        if i > 0:
            printf(" ")
        printf("%d", i)
    printf("\n")
    return 0

main()
