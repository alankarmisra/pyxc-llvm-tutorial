# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '11 22' %t
# RUN: ! grep -q "Error (Line:" %t

struct Point:
    x: i32

def main() -> i32:
    ps: array[Point, 2]
    ps[0].x = 11
    ps[1].x = 22
    print(ps[0].x, ps[1].x)
    return 0

main()
