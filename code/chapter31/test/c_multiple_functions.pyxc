# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '120' %t
# RUN: ! grep -q "Error (Line:" %t

def factorial(n: i32) -> i32:
    if n <= 1:
        return 1
    return n * factorial(n - 1)

def main() -> i32:
    r: i32 = factorial(5)
    print(r)
    return 0

main()
