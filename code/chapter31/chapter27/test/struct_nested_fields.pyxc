# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '7 11' %t
# RUN: ! grep -q "Error (Line:" %t

struct Inner:
    x: i32

struct Outer:
    i: Inner
    y: i32

def main() -> i32:
    o: Outer
    o.i.x = 7
    o.y = 11
    print(o.i.x, o.y)
    return 0

main()
