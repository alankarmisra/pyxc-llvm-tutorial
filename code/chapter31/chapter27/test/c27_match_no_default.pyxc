# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x 'start' %t
# RUN: grep -x 'end' %t
# RUN: ! grep -x 'hit' %t
# RUN: ! grep -q "Error (Line:" %t

def main() -> i32:
    x: i32 = 7
    printf("start\n")
    match x:
        case 1:
            printf("hit\n")
        case 2:
            printf("hit\n")
    printf("end\n")
    return 0

main()
