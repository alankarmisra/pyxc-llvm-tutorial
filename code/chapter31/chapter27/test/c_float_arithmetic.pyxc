# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep 'sum=5.5' %t
# RUN: ! grep -q "Error (Line:" %t

def main() -> i32:
    x: f64 = 2.5
    y: f64 = 3.0
    z: f64 = x + y
    printf("sum=%.1f\n", z)
    return 0

main()
