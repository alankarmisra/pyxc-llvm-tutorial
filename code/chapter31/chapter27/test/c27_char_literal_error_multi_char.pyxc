# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -q "Error (Line:" %t
# RUN: grep -q "Character literal must contain exactly one character" %t

def main() -> i32:
    x: i32 = 'ab'
    print(x)
    return 0

main()
