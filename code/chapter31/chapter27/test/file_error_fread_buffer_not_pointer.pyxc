# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -q "Error (Line:" %t
# RUN: grep -q "fread expects pointer buffer argument" %t

def main() -> i32:
    f: ptr[void] = fopen("chapter23_io_e.bin", "rb")
    x: i32 = 0
    fread(x, 1, 1, f)
    return 0

main()
