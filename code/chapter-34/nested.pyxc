extern def printd(x: float64)
def main() -> int:
  var count: int = 0
  for var i: int = 0, i < 3, 1:
    for var j: int = 0, j < 3, 1:
      if j == 1:
        break
      count++
  printd(float64(count))
  return 0
