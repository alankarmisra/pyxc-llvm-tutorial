# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 20.000000

extern def printd(x: float64)

struct Pair:
  a: int
  b: int

def main() -> int:
  var t: Pair
  t.a = 10
  t.b = 20
  var p: ptr[int] = addr(t.a)
  var q: ptr[int] = 1 + p
  printd(float64(q[0]))
  return 0
