# RUN: %pyxc --emit exe %s %S/Inputs/foo_module.pyxc -o %t
# RUN: %t | FileCheck %s --check-prefix=OUT

# OUT: 42.000000

extern def foo() -> float64
extern def printd(x: float64) -> float64

def main() -> None:
    printd(foo())
    return
