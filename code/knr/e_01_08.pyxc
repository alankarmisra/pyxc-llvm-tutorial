type string = ptr[int8]
extern def getchar() -> int32
extern def printf(fmt: string, ...) -> int

var EOF: int32 = -1

def main() -> int:
    var c:int32, nb:int, nt:int, nl:int # default initialized to 0
    
    while (c = getchar()) != EOF:
        if c == ' ':
            ++nb
        elif c == '\t':
            ++nt
        elif c == '\n':
            ++nl

    printf("spaces = %d, tabs = %d, newlines = %d\n", nb, nt, nl)    
    return 0
