# RUN: %pyxc < %s 2>&1 | FileCheck %s --check-prefix=CHECK
# CHECK-DAG: {{Parsed a function definition}}
# CHECK-DAG: {{ret double %x}}

# NameExpressionNode::codegen: a parameter reference looks up NamedValues and
# returns the Value* for the argument. The IR must name the argument 'x' and
# return it directly — no instruction needed.
def identity(x): x
