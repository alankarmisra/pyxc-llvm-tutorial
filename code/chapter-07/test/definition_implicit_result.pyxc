# RUN: %pyxc < %s 2>&1 | FileCheck %s --check-prefix=CHECK --implicit-check-not="{{Error \\(Line}}"
# CHECK-DAG: {{Parsed a function definition}}

# definition: the body expression is the function's implicit result
def foo(x): x + 1
