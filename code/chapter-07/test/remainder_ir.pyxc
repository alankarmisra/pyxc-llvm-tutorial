# RUN: %pyxc < %s 2>&1 | FileCheck %s --check-prefix=CHECK --implicit-check-not="{{Error \\(Line}}"
# CHECK-DAG: {{frem double}}

# BinaryExpressionNode::codegen: remainder emits frem for double operands.
def remainder(x, y): x % y
