# RUN: %pyxc < %s 2>&1 | FileCheck %s --check-prefix=CHECK
# CHECK-DAG: {{Error}}
# CHECK-DAG: Unknown function: 'notdeclared'

# CallExpressionNode::codegen error path: TheModule->getFunction returns nullptr when
# the callee has never been declared or defined.
def bad(x): notdeclared(x)
