# RUN: %pyxc < %s 2>&1 | FileCheck %s --check-prefix=CHECK
# CHECK-DAG: {{Parsed a function definition}}
# CHECK-DAG: {{define double @f}}

# FunctionSignatureNode::codegen: return type is always double regardless of arity.
# The 'define double' prefix must appear for every function definition.
def f(x): x
