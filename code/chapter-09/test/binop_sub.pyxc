# RUN: %pyxc -v < %s 2>&1 | FileCheck %s --check-prefix=CHECK
# CHECK-DAG: {{Parsed a function definition}}
# CHECK-DAG: {{fsub double}}

# BinaryExpressionNode::codegen '-': CreateFSub emits an fsub instruction.
def sub(a, b): a - b
