# RUN: %pyxc -v < %s 2>&1 | FileCheck %s --check-prefix=CHECK
# CHECK-DAG: {{Parsed a function definition}}
# CHECK-DAG: {{fadd double}}

# BinaryExpressionNode::codegen '+': CreateFAdd emits an fadd instruction.
# Parameters are non-constant so folding does not apply.
def add(a, b): a + b
