# RUN: %pyxc < %s 2>&1 | FileCheck %s --check-prefix=CHECK
# CHECK-DAG: {{Error}}
# CHECK-DAG: {{Evaluated to 1.000000}}

# Error recovery + JIT: a parse error on one line must not prevent subsequent
# inputs from compiling and executing. The REPL must recover and evaluate the
# next expression correctly.
def bad(x) x
1
