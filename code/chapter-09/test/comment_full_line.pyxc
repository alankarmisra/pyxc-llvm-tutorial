# RUN: %pyxc < %s 2>&1 | FileCheck %s --check-prefix=CHECK
# CHECK-DAG: {{Parsed a top-level expression.}}

# top: full-line comment is skipped; expression on next line parses normally
# this is a comment
1 + 2
