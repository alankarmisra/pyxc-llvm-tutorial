# RUN: %pyxc < %s 2>&1 | FileCheck %s --check-prefix=CHECK --implicit-check-not="{{Error \\(Line}}"
# CHECK-DAG: {{Evaluated to -3.000000}}
# CHECK-DAG: {{Evaluated to 5.000000}}

# JIT execution: unary minus works for grouped and recursively negated values.
-(1 + 2)
--5
