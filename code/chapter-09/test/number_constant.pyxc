# RUN: %pyxc -v < %s 2>&1 | FileCheck %s --check-prefix=CHECK
# CHECK-DAG: {{Parsed a top-level expression}}
# CHECK-DAG: {{ret double}}

# NumberExpressionNode::codegen: a literal becomes a ConstantFP.
# The anon function must contain a ret double instruction.
42.0
