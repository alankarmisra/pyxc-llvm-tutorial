# RUN: %pyxc -v < %s 2>&1 | FileCheck %s --check-prefix=CHECK
# CHECK-DAG: {{Parsed a function definition}}
# CHECK-DAG: {{fdiv double}}

# BinaryExpressionNode::codegen '/': CreateFDiv emits an fdiv instruction.
def div(a, b): a / b
