# RUN: %pyxc < %s 2>&1 | FileCheck %s --check-prefix=CHECK --implicit-check-not="{{define double @bad}}"
# CHECK-DAG: {{Error}}
# CHECK-DAG: {{Unknown variable name}}

# FunctionDefinitionNode::codegen: when Body->codegen() fails, eraseFromParent() removes
# the partially-built function. The final module dump must not contain a
# 'define double @bad' — no broken declaration is left behind.
def bad(x): z
