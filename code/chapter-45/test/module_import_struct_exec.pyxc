# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 7.000000

module app.main
import app.shapes

extern def printd(x: float64) -> float64

def main() -> int:
  var p: Point
  p.x = 7
  printd(float64(p.x))
  return 0
