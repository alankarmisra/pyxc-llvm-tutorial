# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '99' %t
# RUN: ! grep -q "Error (Line:" %t

def main() -> i32:
    a: array[i32, 3]
    a[0] = 1
    a[1] = 2
    a[2] = 3
    a[1] = 99
    print(a[1])
    return 0

main()
