# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '7' %t
# RUN: grep -x '3' %t
# RUN: grep -x '1' %t
# RUN: ! grep -q "Error (Line:" %t

def main() -> i32:
    print(1 | 2 & 4 | 6)
    print(1 ^ 3 & 2)
    print(1 | 2 ^ 3)
    return 0

main()
