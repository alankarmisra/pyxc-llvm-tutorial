# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '42' %t
# RUN: ! grep -q "Error (Line:" %t

# This is a comment
def main() -> i32:
    # Another comment
    x: i32 = 42 # inline comment
    print(x)
    return 0

main()
