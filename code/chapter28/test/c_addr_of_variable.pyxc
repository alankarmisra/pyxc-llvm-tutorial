# Test addr() to get address of a variable, then index through it.
# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '77' %t
# RUN: ! grep -q "Error (Line:" %t

def main() -> i32:
    x: i32 = 77
    p: ptr[i32] = addr(x)
    print(p[0])
    return 0

main()
