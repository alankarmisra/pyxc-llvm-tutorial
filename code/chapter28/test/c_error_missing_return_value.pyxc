# Test that bare return in non-void function is rejected.
# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -q "Missing return value" %t

def foo() -> i32:
    return

def main() -> i32:
    foo()
    return 0

main()
