# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '10 20' %t
# RUN: ! grep -q "Error (Line:" %t

struct Point:
    x: i32
    y: i32

def main() -> i32:
    p: Point
    p.x = 10
    p.y = 20
    print(p.x, p.y)
    return 0

main()
