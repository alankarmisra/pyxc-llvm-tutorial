# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -q "Error (Line:" %t
# RUN: grep -q "Modulo operator '%' requires integer operands" %t

def main() -> i32:
    a: f32 = 10.0
    b: i32 = 3
    print(a % b)
    return 0

main()
