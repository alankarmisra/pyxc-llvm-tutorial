# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x 'two' %t
# RUN: ! grep -q "Error (Line:" %t

def main() -> i32:
    x: i32 = 2
    match x:
        case 1:
            printf("one\n")
        case 2:
            printf("two\n")
        case _:
            printf("other\n")
    return 0

main()
