# RUN: printf 'hello\n' | %pyxc -i %s > %t 2>&1
# RUN: grep -x 's=hello' %t
# RUN: ! grep -q "Error (Line:" %t

def main() -> i32:
    s: ptr[i8] = malloc[i8](64)
    scanf("%s", s)
    printf("s=%s\n", s)
    free(s)
    return 0

main()
