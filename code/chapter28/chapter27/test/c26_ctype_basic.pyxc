# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '1 1 1' %t
# RUN: grep -x 'a Z' %t
# RUN: ! grep -q "Error (Line:" %t

def main() -> i32:
    printf("%d %d %d\n", isdigit(57), isalpha(65), isspace(32))
    printf("%c %c\n", tolower(65), toupper(122))
    return 0

main()
