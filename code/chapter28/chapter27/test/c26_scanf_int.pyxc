# RUN: printf '42\n' | %pyxc -i %s > %t 2>&1
# RUN: grep -x 'x=42' %t
# RUN: ! grep -q "Error (Line:" %t

def main() -> i32:
    x: i32 = 0
    scanf("%d", addr(x))
    printf("x=%d\n", x)
    return 0

main()
