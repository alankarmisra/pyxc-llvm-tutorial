# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -q "Error (Line:" %t
# RUN: grep -q "match expression must be an integer type" %t

def main() -> i32:
    x: f64 = 2.0
    match x:
        case 2:
            printf("two\n")
    return 0

main()
