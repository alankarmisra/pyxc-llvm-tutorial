# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '7 9' %t
# RUN: ! grep -q "Error (Line:" %t

type I2 = array[i32, 2]

def main() -> i32:
    a: I2
    a[0] = 7
    a[1] = 9
    print(a[0], a[1])
    return 0

main()
