# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -q "Error (Line:" %t
# RUN: grep -q "fopen expects pointer mode argument" %t

def main() -> i32:
    mode: i32 = 1
    f: ptr[void] = fopen("chapter23_io_c.txt", mode)
    return 0

main()
