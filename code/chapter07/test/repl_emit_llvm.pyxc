# RUN: %pyxc repl --emit=llvm-ir < %s > %t 2>&1
# RUN: grep -q "define double @square" %t
# RUN: grep -q "define double @__anon_expr" %t
# RUN: grep -q "call double @square" %t

def square(x):
  return x * x

square(3)
