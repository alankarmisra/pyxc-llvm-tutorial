# RUN: %pyxc repl --emit=llvm-ir < %s > %t 2>&1
# RUN: grep -q "define double @fold_in_function" %t
# RUN: grep -q "fadd double %x, 6.000000e+00" %t
# RUN: not grep -q "fmul double 2.000000e+00, 3.000000e+00" %t

def fold_in_function(x):
  return x + (2 * 3)
