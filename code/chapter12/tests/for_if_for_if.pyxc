extern def printd(x)

def main():
    for i in range(0, 2):
        if(i < 1):
            for j in range(0, 2):
                if(j < 1):
                    printd(j)
                else:
                    printd(j + 10)
        else:
            printd(i)

main()
