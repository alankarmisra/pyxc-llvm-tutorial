# Test addr() on an array element.
# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '50' %t
# RUN: ! grep -q "Error (Line:" %t

def main() -> i32:
    a: array[i32, 3]
    a[0] = 10
    a[1] = 50
    a[2] = 90
    p: ptr[i32] = addr(a[1])
    print(p[0])
    return 0

main()
