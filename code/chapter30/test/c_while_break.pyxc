# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '5' %t
# RUN: ! grep -q "Error (Line:" %t

def main() -> i32:
    i: i32 = 0
    while i < 100:
        if i == 5:
            break
        i = i + 1
    print(i)
    return 0

main()
