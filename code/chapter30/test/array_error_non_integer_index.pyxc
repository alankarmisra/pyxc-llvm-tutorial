# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -q "Error (Line:" %t
# RUN: grep -q "Array index must be an integer type" %t

def main() -> i32:
    a: array[i32, 2]
    idx: f64 = 1.0
    print(a[idx])
    return 0

main()
