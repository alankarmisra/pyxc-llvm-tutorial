# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '0' %t
# RUN: grep -x '1' %t
# RUN: grep -x '2' %t
# RUN: ! grep -x '3' %t
# RUN: ! grep -q "Error (Line:" %t

def main() -> i32:
    i: i32 = 0
    do:
        print(i)
        i = i + 1
        if i == 3:
            break
    while i < 10
    return 0

main()
