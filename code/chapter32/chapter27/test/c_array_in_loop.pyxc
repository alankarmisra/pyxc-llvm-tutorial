# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '0 1 4 9 16' %t
# RUN: ! grep -q "Error (Line:" %t

def main() -> i32:
    a: array[i32, 5]
    for i in range(0, 5):
        a[i] = i * i
    print(a[0], a[1], a[2], a[3], a[4])
    return 0

main()
