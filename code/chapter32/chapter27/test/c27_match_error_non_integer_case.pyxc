# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -q "Error (Line:" %t
# RUN: grep -q "match case expression must be an integer type" %t

def main() -> i32:
    x: i32 = 2
    match x:
        case 2.0:
            printf("two\n")
    return 0

main()
