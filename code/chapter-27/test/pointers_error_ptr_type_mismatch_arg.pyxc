# RUN: not %pyxc --emit llvm-ir -o %t.ll %s 2>&1 | FileCheck %s
# CHECK: {{[Ee]rror}}
# CHECK: {{[Ee]xpects|type|mismatch|pointer}}

def consume(p: ptr[int]) -> int:
  return p[0]

def main() -> int:
  var x: float64 = 3.14
  return consume(addr(x))
