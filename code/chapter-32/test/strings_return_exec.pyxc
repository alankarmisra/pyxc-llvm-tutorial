# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: returned

extern def puts(s: ptr[int8]) -> int

def greeting() -> ptr[int8]:
  return "returned"

def main() -> int:
  puts(greeting())
  return 0
