# RUN: %pyxc -i %s > %t 2>&1
# RUN: ! grep -q "Error" %t

extern def printd(x)

def main():
    for i in range(0, 2):
        if(i < 1):
            for j in range(0, 2):
                printd(j)
        else:
            printd(i)

main()
