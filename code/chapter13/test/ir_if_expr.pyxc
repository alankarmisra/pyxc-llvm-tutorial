# RUN: %pyxc repl --emit=llvm-ir < %s > %t 2>&1
# RUN: grep -q "define double @choose" %t
# RUN: grep -q "phi double" %t
# RUN: grep -q "fcmp one double" %t

def choose(x):
  return if x > 0:
    x
  else:
    0 - x
