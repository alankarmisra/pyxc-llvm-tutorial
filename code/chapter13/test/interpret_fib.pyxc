# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -q "34.000000" %t
# RUN: ! grep -q "Error (Line:" %t

extern def printd(x)

def fib(x):
    if(x < 3):
        return 1
    else:
        return fib(x-1) + fib(x-2)

printd(fib(9))
