# RUN: %pyxc repl --emit=llvm-ir < %s > %t 2>&1
# RUN: grep -q "fadd double" %t
# RUN: grep -q "fcmp ueq double" %t
# RUN: grep -q "define double @test_prec" %t

def test_prec(x, y):
    return x + 1 == y
