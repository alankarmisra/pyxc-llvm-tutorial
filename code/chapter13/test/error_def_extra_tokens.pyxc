# RUN: %pyxc < %s > %t 2>&1
# RUN: grep -q "Error (" %t
# RUN: grep -q "Expected newline after statement" %t
# RUN: ! grep -q "define double @foo" %t
# Test that extra tokens after a def don't generate IR

def foo(x):
  return x 42
