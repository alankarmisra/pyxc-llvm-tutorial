# RUN: %pyxc %s 2>&1 | FileCheck %s
# CHECK: 3.000000
# CHECK-NOT: ready>


# Tests: blank lines inside an indented block are ignored in file mode.

extern def printd(x: float64) -> float64

def f() -> float64:
    var x: float64 = 1.0

    x = x + 2
    return x

printd(f())
