# RUN: not %pyxc --emit llvm-ir -o %t.ll %s 2>&1 | FileCheck %s
# CHECK: {{[Ee]rror}}
# CHECK: Duplicate trait 'Adder' in class implements list

trait Adder:
  def add(x: int, y: int) -> int

class Bad(Adder, Adder):
  public def add(x: int, y: int) -> int:
    return x + y
