# RUN: %pyxc < %s > %t 2>&1
# RUN: grep -q "fadd double" %t
# RUN: grep -q "fsub double" %t
# RUN: grep -q "fmul double" %t
# RUN: grep -q "fdiv double" %t
# RUN: grep -q "frem double" %t

def test_add(x, y):
    return x + y

def test_sub(x, y):
    return x - y

def test_mul(x, y):
    return x * y

def test_div(x, y):
    return x / y

def test_rem(x, y):
    return x % y
