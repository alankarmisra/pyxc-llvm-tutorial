# RUN: %pyxc < %s > %t 2>&1
# RUN: grep -q "Evaluated to 0.841471" %t
# RUN: grep -q "Evaluated to 1.000000" %t

def addone(x): 
    return x + 1

extern def sin(x)
extern def cos(x)

sin(1.0)

def ident(x): 
    return sin(x)*sin(x)+cos(x)*cos(x)

ident(4.0)
