# RUN: %pyxc %s 2>&1 | FileCheck %s
# CHECK: 6.000000
# CHECK-NOT: ready>


# Tests: file mode with an indented block and a for loop.

extern def printd(x)

def sum_to(n):
    var acc = 0
    for var i = 1, i <= n, 1:
        acc = acc + i
    return acc

printd(sum_to(3))
