# RUN: %pyxc < %s > %t 2>&1
# RUN: grep -c "Evaluated to" %t | grep -q "^3$"

# JIT execution across multiple top-level expressions: each expression gets
# its own module, is compiled, executed, and freed. Three expressions must
# each produce an 'Evaluated to' line — confirms RT->remove() and module
# reinitialisation work correctly across successive inputs.
1
2
3
