# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 8.000000
# CHECK: 16.000000

extern def printd(x: float64)

struct Pair:
  a: int64
  b: int64

def main() -> int:
  printd(float64(sizeof(int64)))
  printd(float64(sizeof(Pair)))
  return 0
