# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 5.000000

extern def printd(x: float64)

class Box:
  value: int

  def set(v: int) -> None:
    self.value = v


def main() -> int:
  var b: Box
  b.value = 1
  b.set(5)
  printd(float64(b.value))
  return 0
