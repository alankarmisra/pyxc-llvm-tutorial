# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 3.140000

extern def printd(x: float64)

struct Circle:
  radius: float64

def main() -> int:
  var c: Circle
  c.radius = 3.14
  printd(c.radius)
  return 0
