# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 7.000000

extern def printd(x: float64)

class Counter:
  value: int

  def get() -> int:
    return self.value

def main() -> int:
  var c: Counter
  c.value = 7
  printd(float64(c.get()))
  return 0
