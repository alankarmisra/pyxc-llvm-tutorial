# RUN: not %pyxc --emit llvm-ir -o %t.ll %s 2>&1 | FileCheck %s
# CHECK: {{[Ee]rror}}
# CHECK: Duplicate trait 'Addable' in class implements list

trait Addable[T]:
  def add(x: T, y: T) -> T

class C(Addable[int], Addable[float64]):
  public def add(x: int, y: int) -> int:
    return x + y
