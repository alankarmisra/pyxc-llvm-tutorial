# RUN: %pyxc --emit llvm-ir -o %t.ll %s
# RUN: FileCheck %s < %t.ll
# CHECK: %struct.Point = type { i64, i64 }

class Point:
  x: int
  y: int

def main() -> int:
  var p: Point
  p.x = 1
  p.y = 2
  return p.x + p.y
