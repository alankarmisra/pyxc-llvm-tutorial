# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 12.000000

extern def printd(x: float64)

class Acc:
  base: int

  def add(x: int) -> int:
    return self.base + x

def main() -> int:
  var a: Acc
  a.base = 9
  printd(float64(a.add(3)))
  return 0
