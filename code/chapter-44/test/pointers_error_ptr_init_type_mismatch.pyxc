# RUN: not %pyxc --emit llvm-ir -o %t.ll %s 2>&1 | FileCheck %s
# CHECK: {{[Ee]rror}}
# CHECK: {{type|mismatch|pointer}}

def main() -> int:
  var x: float64 = 3.14
  var p: ptr[int] = addr(x)
  return 0
