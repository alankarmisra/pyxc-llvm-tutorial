# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: alias basic

extern def puts(s: ptr[int8]) -> int

type string = ptr[int8]

def main() -> int:
  var s: string = "alias basic"
  puts(s)
  return 0
