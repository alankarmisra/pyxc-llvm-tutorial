# RUN: not %pyxc --emit llvm-ir -o %t.ll %s 2>&1 | FileCheck %s
# CHECK: {{[Ee]rror}}
# CHECK: Field 'secret' is private on 'Box'

class Box:
  private secret: int


def main() -> int:
  var b: Box = Box()
  return b.secret
