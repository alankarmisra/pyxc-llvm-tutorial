# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 99.000000

extern def printd(x: float64)

def main() -> int:
  var x: int = 5
  var p: ptr[int] = addr(x)
  p[0] = 99
  printd(float64(x))
  return 0
