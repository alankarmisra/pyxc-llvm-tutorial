# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 6.000000

extern def printd(x: float64)

def main() -> int:
  var x: int = 10
  x += 5
  x -= 3
  x *= 2
  x /= 4
  x %= 10
  printd(float64(x))
  return 0
