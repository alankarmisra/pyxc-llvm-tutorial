# RUN: not %pyxc --emit llvm-ir -o %t.ll %s 2>&1 | FileCheck %s
# CHECK: {{[Ee]rror}}
# CHECK: Method '__init__' is private on 'Box'

class Box:
  private def __init__() -> None:
    return


def main() -> int:
  var b: Box = Box()
  return 0
