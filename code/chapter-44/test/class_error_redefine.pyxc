# RUN: not %pyxc --emit llvm-ir -o %t.ll %s 2>&1 | FileCheck %s
# CHECK: {{[Ee]rror}}
# CHECK: {{already defined}}

class Point:
  x: int

class Point:
  x: int
  y: int

def main() -> int:
  return 0
