# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 7.000000

extern def printd(x: float64)

var g: int[2]

def main() -> int:
  g[0] = 3
  g[1] = 4
  printd(float64(g[0] + g[1]))
  return 0
