# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 4.000000

extern def printd(x: float64)

def main() -> int:
  var i: int = 0
  var sum: int = 0
  while i < 5:
    i += 1
    if i == 3:
      continue
    sum += 1
  printd(float64(sum))
  return 0
