# RUN: not %pyxc --emit llvm-ir -o %t.ll %s 2>&1 | FileCheck %s
# CHECK: {{[Ee]rror}}
# CHECK: Duplicate switch case value

def main() -> int:
  var x: int = 1
  switch x:
    case 1:
      return 1
    case 1:
      return 2
  return 0
