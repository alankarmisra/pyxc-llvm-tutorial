# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 9.000000

extern def printd(x: float64)

struct Inner:
  value: int

struct Outer:
  inner: Inner

def main() -> int:
  var o: Outer
  o.inner.value = 9
  printd(float64(o.inner.value))
  return 0
