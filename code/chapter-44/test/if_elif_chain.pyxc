# RUN: %pyxc < %s 2>&1 | FileCheck %s
# CHECK: 1.000000
# CHECK: 0.000000
# CHECK: -1.000000

# Tests: Python-style if/elif/else chain.

def sign(x: float64) -> float64:
    if x > 0:
        return 1.0
    elif x == 0:
        return 0.0
    else:
        return -1.0

sign(5)
sign(0)
sign(-2)
