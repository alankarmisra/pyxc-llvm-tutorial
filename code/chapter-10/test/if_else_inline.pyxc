# RUN: %pyxc < %s 2>&1 | FileCheck %s
# CHECK: Parsed a function definition.
# CHECK: Evaluated to 10.000000


# Tests: inline if/else (condition, then, and else all on one line).
# if-expression = "if" expression ":" [ end-of-lines ] expression [ end-of-lines ] "else" ":" [ end-of-lines ] expression

def maxval(a, b): if a > b: a else: b

maxval(10, 5)
