# RUN: %pyxc < %s > %t 2>&1
# RUN: grep -q "Error (Line" %t
# RUN: grep -q "Expected '(' in function signature" %t
# RUN: grep -q "def foo x" %t

# function signature: no '(' after function name
def foo x: x
