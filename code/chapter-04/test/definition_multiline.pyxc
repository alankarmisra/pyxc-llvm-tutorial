# RUN: %pyxc < %s 2>&1 | FileCheck %s --check-prefix=CHECK
# CHECK-DAG: {{Parsed a function definition.}}

# definition: body on the next line (the [ end-of-lines ] optional in the grammar)
def foo(x):
  x + 1
