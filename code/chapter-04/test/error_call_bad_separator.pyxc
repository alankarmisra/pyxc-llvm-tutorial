# RUN: %pyxc < %s > %t 2>&1
# RUN: grep -q "Error (Line" %t
# RUN: grep -q "Expected ')' or ',' in argument list" %t
# RUN: grep -q "foo(1" %t

# identifierexpr: arguments separated by space instead of comma
foo(1 2)
