# RUN: %pyxc < %s > %t 2>&1
# RUN: grep -c "Parsed a top-level expression." %t | grep -q "5"
# RUN: not grep -q "Error:" %t

# expression/sum/term: each supported binary operator
1 + 2
3 - 4
5 * 6
8 / 2
1 < 2
