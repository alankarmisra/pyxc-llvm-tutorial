# RUN: %pyxc < %s > %t 2>&1
# RUN: grep -q "Error (Line" %t
# RUN: grep -q "Expected \`def\` after extern" %t
# RUN: grep -q "extern foo" %t

# external: 'def' keyword is absent after 'extern'
extern foo(x)
