# RUN: %pyxc < %s > %t 2>&1
# RUN: grep -q "Error (Line" %t
# RUN: grep -q "Unexpected '='" %t
# RUN: grep -q "Parsed a top-level expression." %t

# Test that the REPL recovers after a parse error and continues parsing.
# '3 = 10' fails because '=' is not a valid primary expression.
# HandleTopLevelExpression skips tokens till the end of the line.
# The second line then parses successfully.
3 = 10
1 + 2 * 3
