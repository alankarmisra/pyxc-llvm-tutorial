# RUN: %pyxc < %s > %t 2>&1
# RUN: grep -q "Error (Line" %t
# RUN: grep -q "Expected 'return' in function body" %t
# RUN: grep -q "def foo(x):" %t

# definition: body is present but 'return' keyword is missing
def foo(x): x + 1
