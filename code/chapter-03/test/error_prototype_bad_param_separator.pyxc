# RUN: %pyxc < %s > %t 2>&1
# RUN: grep -q "Error (Line" %t
# RUN: grep -q "Expected ')' or ',' in parameter list" %t
# RUN: grep -q "def foo(x y)" %t

# prototype: parameters separated by space instead of comma
def foo(x y): return x
