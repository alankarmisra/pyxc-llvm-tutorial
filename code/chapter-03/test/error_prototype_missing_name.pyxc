# RUN: %pyxc < %s > %t 2>&1
# RUN: grep -q "Error (Line" %t
# RUN: grep -q "Expected function name in prototype" %t
# RUN: grep -q "def 1" %t

# prototype: first token after 'def' is not an identifier
def 1(x): return x
