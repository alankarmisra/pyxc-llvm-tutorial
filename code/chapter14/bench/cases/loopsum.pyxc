extern def printd(x)

def loopsum(n, m, total):
    for i in range(1, n + 1, 1):
        for j in range(1, m + 1, 1):
            total = total + i * j
    return total

def main():
    return printd(loopsum(10000, 10000, 0))

main()
