# RUN: %pyxc -i %s > %t 2>&1
# RUN: ! grep -q "Error" %t

extern def printd(x)

def main():
    for i in range(0, 3):
        for j in range(0, 2):
            for k in range(0, 1):
                printd(k)

main()
