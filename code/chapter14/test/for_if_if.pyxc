# RUN: %pyxc -i %s > %t 2>&1
# RUN: ! grep -q "Error" %t

extern def printd(x)

def main():
    for i in range(0, 2):
        if(i < 1):
            if(1):
                printd(i)
            else:
                printd(i + 10)
        else:
            printd(i + 20)

main()
