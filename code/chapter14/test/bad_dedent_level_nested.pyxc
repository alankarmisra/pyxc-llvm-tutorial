# RUN: %pyxc -i %s > %t 2>&1 || true
# RUN: grep -q "Error" %t

extern def printd(x)

def main():
    if(1):
        if(1):
            return printd(1)
        else:
            return printd(2)
     return printd(3)

main()
