# RUN: %pyxc -i %s > %t 2>&1
# RUN: ! grep -q "Error" %t

extern def printd(x)

def main():
    if(1):
        for i in range(0, 2):
            for j in range(0, 2):
                printd(j)
    else:
        return printd(0)

main()
