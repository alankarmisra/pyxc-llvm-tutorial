# RUN: not %pyxc --emit llvm-ir -o %t.ll %s 2>&1 | FileCheck %s
# CHECK: {{[Ee]rror}}
# CHECK: {{argument|expects|mismatch}}

def consume(p: ptr[int]) -> int:
  return p[0]

def main() -> int:
  var a: float64[2]
  return consume(a)
