# RUN: %pyxc -i %s > %t 2>&1
# RUN: ! grep -q "Error (Line:" %t

def is_even(n: i32) -> i32:
    if n == 0:
        return 1
    return is_odd(n - 1)

def is_odd(n: i32) -> i32:
    if n == 0:
        return 0
    return is_even(n - 1)

def main() -> i32:
    return is_even(10)

main()
