extern def printd(x)
extern def putchard(c)

def f_to_c(fahr):
    return (5 * (fahr - 32)) / 9

def main():
    for fahr in range(0, 101, 10):
        printd(fahr)
        printd(f_to_c(fahr))
    putchard(10)
    return 0

main()