# RUN: %pyxc < %s > %t 2>&1
# RUN: ! grep -q "^Error:" %t
# RUN: grep -q "define double @iseven" %t
# RUN: grep -q "define double @isodd" %t
# RUN: grep -q "call double @isodd" %t
# RUN: grep -q "call double @iseven" %t

extern def printd(x)

def iseven(n) : if n < 1 : return 1 else : return isodd(n-1)

def isodd(n) : if n < 1 : return 0 else : return iseven(n-1)

def main() : return printd(iseven(10))

main()
