# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -q "30.000000" %t
# RUN: ! grep -q "Error (Line:" %t

extern def printd(x)

def pick(x):
    if(x < 1):
        return 10
    else:
        if(x < 2):
            return 20
        else:
            return 30

printd(pick(2))
