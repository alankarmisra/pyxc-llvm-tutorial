# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 42.000000

extern def printd(x: float64)

type IntPointer = ptr[int]

def main() -> int:
  var value: int = 42
  var pointer: IntPointer = addr(value)
  printd(float64(pointer[0]))
  return 0
