# RUN: %pyxc < %s > %t 2>&1
# RUN: grep -q "Evaluated to 1.000000" %t
# RUN: test "$(grep -c 'Evaluated to 0.000000' %t | tr -d '[:space:]')" = "2"

def lt2(x):
    if x < 2:
        return 1
    else:
        return 0


lt2(1)
lt2(2)
lt2(3)
