# RUN: %pyxc < %s > %t 2>&1
# RUN: grep -q "Unexpected '='" %t
# RUN: test "$(grep -c 'Error (' %t | tr -d '[:space:]')" = "1"
# RUN: test "$(grep -c '7.000000' %t | tr -d '[:space:]')" = "1"

3 = 10
1 + 2 * 3
