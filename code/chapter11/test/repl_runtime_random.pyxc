# RUN: %pyxc repl < %s > %t 2>&1
# RUN: grep -Eq "[0-9]+\\.[0-9]+" %t
# RUN: not grep -q "Unknown function referenced" %t

extern def seedrand(x)
extern def randd(max)
extern def printd(x)

seedrand(123)
printd(randd(10))
