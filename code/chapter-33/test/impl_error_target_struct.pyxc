# RUN: not %pyxc --emit llvm-ir -o %t.ll %s 2>&1 | FileCheck %s
# CHECK: {{[Ee]rror}}
# CHECK: traits can only be implemented on classes

trait Adder:
  def add(x: int, y: int) -> int

struct S:
  value: int

impl Adder for S:
  def add(x: int, y: int) -> int:
    return x + y
