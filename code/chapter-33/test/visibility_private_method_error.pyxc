# RUN: not %pyxc --emit llvm-ir -o %t.ll %s 2>&1 | FileCheck %s
# CHECK: {{[Ee]rror}}
# CHECK: Method 'hidden' is private on 'Box'

class Box:
  private def hidden() -> int:
    return 1


def main() -> int:
  var b: Box = Box()
  return b.hidden()
