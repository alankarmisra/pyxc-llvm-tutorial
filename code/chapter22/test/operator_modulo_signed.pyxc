# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '1 0 -2' %t
# RUN: ! grep -q "Error (Line:" %t

def main() -> i32:
    a: i32 = 10
    b: i32 = 3
    c: i32 = 8
    d: i32 = 4
    e: i32 = -10
    f: i32 = 4
    print(a % b, c % d, e % f)
    return 0

main()
