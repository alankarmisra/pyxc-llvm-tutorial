# RUN: not %pyxc --emit llvm-ir -o %t.ll %s 2>&1 | FileCheck %s
# CHECK: Error
# CHECK: cannot return float64 from function returning int

# Tests: returning the wrong type is rejected.

def f() -> int:
    return 1.5
