# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 36.000000

extern def printd(x: float64)

struct Point:
  x: int


def main() -> int:
  var p: Point
  p.x = 10
  p.x += 2

  var arr: int[2] = [4, 8]
  arr[1] *= 3

  printd(float64(p.x + arr[1]))
  return 0
