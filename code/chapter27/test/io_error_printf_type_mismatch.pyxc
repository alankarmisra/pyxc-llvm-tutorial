# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -q "Error (Line:" %t
# RUN: grep -q "printf type mismatch for integer format" %t

def main() -> i32:
    s: ptr[i8] = "text"
    printf("x=%d\n", s)
    return 0

main()
