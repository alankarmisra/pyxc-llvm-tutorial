# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -q "Error (Line:" %t
# RUN: grep -q "read expects integer fd argument" %t

def main() -> i32:
    b: ptr[i8] = malloc[i8](8)
    read(b, b, 4)
    free(b)
    return 0

main()
