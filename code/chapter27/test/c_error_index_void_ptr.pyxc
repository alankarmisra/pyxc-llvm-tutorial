# Test that indexing through ptr[void] is rejected.
# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -q "Cannot index through ptr\[void\]" %t

def main() -> i32:
    p: ptr[void] = malloc[i8](10)
    p[0] = 5
    free(p)
    return 0

main()
