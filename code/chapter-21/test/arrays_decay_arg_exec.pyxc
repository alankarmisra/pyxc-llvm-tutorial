# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 12.000000

extern def printd(x: float64)

def read2(p: ptr[int]) -> int:
  return p[1]

def main() -> int:
  var a: int[3]
  a[1] = 12
  printd(float64(read2(a)))
  return 0
