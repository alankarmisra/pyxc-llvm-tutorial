# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 11.000000

extern def printd(x: float64)

def at1(p: ptr[int]) -> int:
  return p[1]

def main() -> int:
  var a: int[3] = [9, 11, 13]
  printd(float64(at1(a)))
  return 0
