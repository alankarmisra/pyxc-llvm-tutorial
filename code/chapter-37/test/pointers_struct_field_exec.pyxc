# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 11.000000

extern def printd(x: float64)

struct Point:
  x: int
  y: int

def main() -> int:
  var p: Point
  p.x = 11
  var px: ptr[int] = addr(p.x)
  printd(float64(px[0]))
  return 0
