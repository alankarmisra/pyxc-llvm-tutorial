# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 60.000000

extern def printd(x: float64)

def main() -> int:
  var a: int[3]
  a[0] = 10
  a[1] = 20
  a[2] = 30
  printd(float64(a[0] + a[1] + a[2]))
  return 0
