# RUN: not %pyxc --emit llvm-ir -o %t.ll %s 2>&1 | FileCheck %s
# CHECK: {{[Ee]rror}}
# CHECK: Unknown trait 'Nope'

class Calc:
  public value: int

impl Nope for Calc:
  def add(x: int, y: int) -> int:
    return x + y
