# RUN: %pyxc repl --emit-llvm < %s > %t 2>&1
# RUN: grep -q "define double @add(double %a, double %b)" %t
# RUN: grep -q "fadd double %a, %b" %t
# RUN: grep -q "ret double" %t

def add(a, b):
    return a + b
