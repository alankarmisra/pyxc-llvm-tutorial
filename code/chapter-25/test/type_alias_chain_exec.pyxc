# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 42.000000

extern def printd(x: float64)

type MyInt = int
type Score = MyInt

def main() -> int:
  var x: Score = 42
  printd(float64(x))
  return 0
