# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 1.000000

extern def printd(x: float64)

struct Flag:
  enabled: bool

def main() -> int:
  var f: Flag
  f.enabled = True
  if f.enabled:
    printd(1.0)
  else:
    printd(0.0)
  return 0
