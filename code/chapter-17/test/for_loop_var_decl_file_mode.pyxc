# RUN: %pyxc %s 2>&1 | FileCheck %s
# CHECK: 1.000000
# CHECK: 2.000000
# CHECK: 3.000000
# CHECK-NOT: ready>
# CHECK-NOT: Parsed

# Tests: for loop with optional 'var' in file mode.

extern def printd(x)

def count(n):
    for var i = 1, i <= n, 1:
        printd(i)
    return 0

count(3)
