# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 99.000000
# CHECK: 1.000000

# Tests: a function parameter with the same name as a global shadows the global
# inside the function body. The global is unchanged after the call.

extern def printd(x)

var x = 1

def use_param(x):
    printd(x)

def main():
    use_param(99)
    printd(x)
