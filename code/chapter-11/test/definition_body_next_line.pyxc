# RUN: %pyxc < %s 2>&1 | FileCheck %s
# CHECK: Parsed a function definition.
# CHECK: Evaluated to 3.000000


# Tests: definition body on the line after ':'.
# definition = "def" function signature ":" [ eols ] expression

def inc(x):
x + 1

inc(2)
