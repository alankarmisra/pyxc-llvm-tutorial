# RUN: %pyxc < %s 2>&1 | FileCheck %s
# CHECK: Parsed a user-defined operator.
# CHECK: conflicts with an existing unary operator


# Tests: cross-arity reuse is rejected.
# After defining unary '%', defining binary '%' must fail.

@unary
def %(x): return x

@binary(5)
def %(x, y): return x + y
