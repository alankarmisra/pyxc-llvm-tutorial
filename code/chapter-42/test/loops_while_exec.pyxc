# RUN: %pyxc < %s 2>&1 | FileCheck %s
# CHECK: 10.000000

def sum_to_four() -> float64:
  var i: float64 = 0
  var sum: float64 = 0
  while i < 5:
    sum = sum + i
    i = i + 1
  return sum

sum_to_four()
