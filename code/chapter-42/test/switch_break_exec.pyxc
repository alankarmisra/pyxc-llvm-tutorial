# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 1.000000

extern def printd(x: float64)

def main() -> int:
  var x: int = 2
  var out: int = 0
  switch x:
    case 2:
      out = 1
      break
      out = 7
    default:
      out = 9
  printd(float64(out))
  return 0
