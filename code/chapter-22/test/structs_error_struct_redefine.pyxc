# RUN: not %pyxc --emit llvm-ir -o %t.ll %s 2>&1 | FileCheck %s
# CHECK: {{[Ee]rror}}
# CHECK: {{already|redefin}}

struct Point:
  x: int

struct Point:
  x: int
  y: int

def bad() -> int:
  return 0
