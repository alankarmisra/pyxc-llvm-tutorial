# RUN: rm -f %t.exe
# RUN: %pyxc -o %t.exe %s > %t.build 2>&1
# RUN: %t.exe alpha beta > %t.out 2>&1
# RUN: grep -x 'argc=3' %t.out
# RUN: grep -x 'argv1=alpha argv2=beta' %t.out
# RUN: ! grep -q "Error (Line:" %t.build

def main(argc: i32, argv: ptr[ptr[i8]]) -> i32:
    printf("argc=%d\n", argc)
    if argc > 2:
        printf("argv1=%s argv2=%s\n", argv[1], argv[2])
    return 0
