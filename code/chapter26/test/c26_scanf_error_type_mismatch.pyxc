# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -q "Error (Line:" %t
# RUN: grep -q "scanf type mismatch for float format" %t

def main() -> i32:
    x: i32 = 0
    scanf("%f", addr(x))
    return 0

main()
