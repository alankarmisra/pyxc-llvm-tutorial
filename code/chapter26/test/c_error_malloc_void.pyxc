# Test that malloc[void] is rejected.
# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -q "malloc element type cannot be void" %t

def main() -> i32:
    p: ptr[void] = malloc[void](10)
    free(p)
    return 0

main()
