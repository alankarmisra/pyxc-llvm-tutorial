# Test that default type aliases (int, char, float, double) work.
# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '42 65' %t
# RUN: ! grep -q "Error (Line:" %t

def main() -> i32:
    x: int = 42
    c: char = 65
    print(x, c)
    return 0

main()
