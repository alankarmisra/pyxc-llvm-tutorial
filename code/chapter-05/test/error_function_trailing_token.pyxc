# RUN: %pyxc < %s 2>&1 | FileCheck %s --check-prefix=CHECK --implicit-check-not="{{Parsed a function definition.}}"
# CHECK-DAG: {{Unexpected ';'}}

# program: a function definition must end at the line boundary
def bad(x): x;
