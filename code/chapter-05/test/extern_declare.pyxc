# RUN: %pyxc < %s > %t 2>&1
# RUN: grep -q "Parsed an extern" %t
# RUN: grep -q "declare double @cos" %t

# PrototypeAST::codegen via extern: ExternalLinkage + no body = 'declare'.
# The IR must contain a declare (not define) for the external function.
extern def cos(x)
