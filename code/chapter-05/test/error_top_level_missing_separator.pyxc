# RUN: %pyxc < %s 2>&1 | FileCheck %s --check-prefix=CHECK --implicit-check-not="{{Parsed a top-level expression.}}"
# CHECK-DAG: {{Unexpected number}}

# program: adjacent top-level expressions require an end-of-line separator
1 2
