# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 9.000000

extern def printd(x: float64)

def first_plus_last(p: ptr[int]) -> int:
  return p[0] + p[2]

def main() -> int:
  var a: int[3] = [4, 1, 5]
  printd(float64(first_plus_last(a)))
  return 0
