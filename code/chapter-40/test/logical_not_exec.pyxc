# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 2.000000

extern def printd(x: float64)

def main() -> int:
  var a: bool = True
  var b: bool = !a
  if b:
    printd(1.0)
  else:
    printd(2.0)
  return 0
