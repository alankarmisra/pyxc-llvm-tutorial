# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 1.000000

extern def printd(x: float64)

def main() -> int:
  var i: int = 0
  do:
    i += 1
  while i < 1
  printd(float64(i))
  return 0
