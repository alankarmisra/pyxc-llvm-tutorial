# RUN: %pyxc < %s > %t 2>&1
# RUN: grep -q "Parsed a function definition" %t
# RUN: grep -q "fmul double" %t

# BinaryExprAST::codegen '*': CreateFMul emits an fmul instruction.
def mul(a, b): return a * b
