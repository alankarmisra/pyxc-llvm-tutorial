# RUN: %pyxc < %s > %t 2>&1
# RUN: grep -q "Parsed a function definition" %t
# RUN: grep -q "call double @double_it" %t

# CallExprAST::codegen: callee looked up by name in TheModule; emits a call
# instruction against it. Both the callee definition and the caller must appear.
def double_it(x): return x + x
def caller(a): return double_it(a)
