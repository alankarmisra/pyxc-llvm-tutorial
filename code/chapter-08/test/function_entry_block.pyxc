# RUN: %pyxc < %s 2>&1 | FileCheck %s --check-prefix=CHECK
# CHECK-DAG: {{Parsed a function definition}}
# CHECK-DAG: {{entry:}}
# CHECK-DAG: {{ret double}}

# FunctionDefinitionNode::codegen: every function body begins with a basic block named
# 'entry' and ends with a ret instruction. Both must appear in the IR.
def f(x): x
