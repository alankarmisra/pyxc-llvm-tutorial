# RUN: %pyxc < %s 2>&1 | FileCheck %s --check-prefix=CHECK
# CHECK-DAG: {{Evaluated to 42}}

# JIT execution: a literal top-level expression must be evaluated and the
# result printed. Confirms the JIT is running and returning the right value.
42.0
