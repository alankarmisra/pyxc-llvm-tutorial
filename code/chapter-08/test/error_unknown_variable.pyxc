# RUN: %pyxc < %s 2>&1 | FileCheck %s --check-prefix=CHECK
# CHECK-DAG: {{Error}}
# CHECK-DAG: {{Unknown variable name: z}}

# Error paths still work with JIT: a reference to an undeclared variable
# must produce an error, not a crash or silent wrong result.
def bad(x): z
