# RUN: %pyxc < %s 2>&1 | FileCheck %s --check-prefix=CHECK --implicit-check-not="{{fadd}}"
# CHECK-DAG: {{Parsed a top-level expression}}
# CHECK-DAG: {{ret double 9}}

# NumberExpressionNode::codegen: two constants folded at IR construction time.
# IRBuilder recognises both operands are ConstantFP and returns a single
# constant for 9.0 — no fadd instruction is emitted.
4 + 5
