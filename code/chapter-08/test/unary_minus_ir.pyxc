# RUN: %pyxc < %s 2>&1 | FileCheck %s --check-prefix=CHECK --implicit-check-not="{{Error \\(Line}}"
# CHECK-DAG: {{fneg double}}

# UnaryExpressionNode::codegen: unary minus emits fneg for a nonconstant value.
def negate(x): -x
