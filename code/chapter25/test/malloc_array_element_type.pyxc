# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '1 2 3 4' %t
# RUN: ! grep -q "Error (Line:" %t

def main() -> i32:
    p: ptr[array[i32, 4]] = malloc[array[i32, 4]](1)
    p[0][0] = 1
    p[0][1] = 2
    p[0][2] = 3
    p[0][3] = 4
    print(p[0][0], p[0][1], p[0][2], p[0][3])
    free(p)
    return 0

main()
