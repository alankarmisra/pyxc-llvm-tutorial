# RUN: rm -f %t.exe
# RUN: %pyxc -o %t.exe %S/c25_mod_add.pyxc %s > %t.out 2>&1
# RUN: %t.exe > %t.run 2>&1
# RUN: grep -x '9' %t.run
# RUN: ! grep -q "Error (Line:" %t.out

extern def add(a: i32, b: i32) -> i32

def main() -> i32:
    print(add(4, 5))
    return 0

main()
