# RUN: %pyxc < %s 2>&1 | FileCheck %s
# CHECK: Assignment to undeclared variable

# Tests: assigning to an undeclared variable is an error.
# assignment-statement requires the target to already exist as a mutable local (via var).
# assignment-statement = name "=" expression ;

def f():
    x = 1
    return x
