# RUN: %pyxc < %s > %t 2>&1
# RUN: grep -q "Error" %t
# RUN: grep -q "Unknown function referenced" %t

# Error paths still work with JIT: calling a function that was never declared
# or defined must produce an error via getFunction() returning nullptr.
notdeclared(1)
