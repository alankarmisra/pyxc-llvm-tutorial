# RUN: %pyxc -g --emit llvm-ir -o %t.ll %s
# RUN: FileCheck --input-file=%t.ll %s
# CHECK: fadd double
# CHECK: fmul double
# CHECK-NOT: ret double 9.000000e+00

# Tests: -g without -O forces O0 (no constant folding).

def main():
    return (1 + 2) * 3
