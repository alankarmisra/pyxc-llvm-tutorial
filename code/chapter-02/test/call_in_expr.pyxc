# RUN: %pyxc < %s 2>&1 | FileCheck %s --check-prefix=CHECK --implicit-check-not="{{Error:}}"
# CHECK-DAG: {{Parsed a top-level expression.}}

# call-expression: call result used inside a larger expression
foo(1) + bar(2, 3) + baz()
