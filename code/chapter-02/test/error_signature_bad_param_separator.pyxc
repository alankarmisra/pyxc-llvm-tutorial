# RUN: %pyxc < %s > %t 2>&1
# RUN: grep -q "Error: " %t
# RUN: grep -q "Expected ')' or ',' in parameter list" %t

# function signature: parameters separated by space instead of comma
def foo(x y): x
