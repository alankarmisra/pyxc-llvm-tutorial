# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '10 20 30 40' %t
# RUN: ! grep -q "Error (Line:" %t

def main() -> i32:
    a: array[i32, 4]
    a[0] = 10
    a[1] = 20
    a[2] = 30
    a[3] = 40
    print(a[0], a[1], a[2], a[3])
    return 0

main()
