# RUN: %pyxc < %s > %t 2>&1
# RUN: grep -c "Parsed a top-level expression." %t | grep -q "5"
# RUN: not grep -q "Error (Line" %t

# expression/sum/term: */ bind tighter than +/-; < binds loosest
1 + 2 * 3
2 * 3 + 1
8 / 2 + 1
1 + 2 + 3 * 4
3 + 4 < 5 * 6
