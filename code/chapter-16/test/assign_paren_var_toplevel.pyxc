# RUN: %pyxc %s 2>&1 | FileCheck %s
# CHECK: 2.000000
# CHECK-NOT: ready>

# Tests: assignment to a parenthesized variable is accepted at top level too.

extern def printd(x)

var x = 1
(x) = 2
printd(x)
