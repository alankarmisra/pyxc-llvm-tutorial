# RUN: %pyxc --emit llvm-ir -o %t.ll %s
# RUN: FileCheck --input-file=%t.ll %s
# CHECK: alloca double
# CHECK: store double 0.0

# Tests: 'var x: float64' with no initializer zero-initializes to 0.0.

def main() -> None:
    var x: float64
    return
