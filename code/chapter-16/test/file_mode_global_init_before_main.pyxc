# RUN: %pyxc %s 2>&1 | FileCheck %s
# CHECK: 20.000000
# CHECK: 21.000000
# CHECK-NOT: ready>

# Tests: top-level statements run before user-defined main in file mode.

extern def printd(x: float64) -> float64

var x: float64 = 20.0

def main() -> None:
    printd(x)
    x = x + 1
    printd(x)
