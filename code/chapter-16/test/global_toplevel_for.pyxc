# RUN: %pyxc %s 2>&1 | FileCheck %s
# CHECK: 15.000000
# CHECK-NOT: ready>

# Tests: a top-level for loop can accumulate into a global variable.
# sum = 1+2+3+4+5 = 15.

extern def printd(x: float64) -> float64

var sum: float64 = 0.0
for i: float64 = 1, i < 6, 1:
    sum = sum + i
printd(sum)
