# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '10 20 30' %t
# RUN: ! grep -q "Error (Line:" %t

def main() -> i32:
    p: ptr[i32] = malloc[i32](3)
    p[0] = 10
    p[1] = 20
    p[2] = 30
    print(p[0], p[1], p[2])
    free(p)
    return 0

main()
