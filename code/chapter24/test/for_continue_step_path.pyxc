# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '8' %t
# RUN: ! grep -q "Error (Line:" %t

def main() -> i32:
    s: i32 = 0
    for i in range(0, 5):
        if i == 2:
            continue
        s = s + i
    print(s)
    return 0

main()
