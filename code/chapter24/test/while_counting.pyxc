# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '0' %t
# RUN: grep -x '1' %t
# RUN: grep -x '2' %t
# RUN: ! grep -q "Error (Line:" %t

def main() -> i32:
    i: i32 = 0
    while i < 3:
        print(i)
        i = i + 1
    return 0

main()
