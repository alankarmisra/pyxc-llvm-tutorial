# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 20.000000

extern def printd(x: float64)

def main() -> int:
  var x: int = 2
  var out: int = 0
  switch x:
    case 1:
      out = 10
    case 2:
      out = 20
    default:
      out = 30
  printd(float64(out))
  return 0
