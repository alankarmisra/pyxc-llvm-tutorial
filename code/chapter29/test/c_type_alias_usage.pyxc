# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '42' %t
# RUN: ! grep -q "Error (Line:" %t

type num = i32

def double_it(x: num) -> num:
    return x * 2

def main() -> i32:
    r: num = double_it(21)
    print(r)
    return 0

main()
