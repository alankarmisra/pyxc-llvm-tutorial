# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '42' %t
# RUN: ! grep -q "Error (Line:" %t

struct Point:
    x: i32

type P = Point

def main() -> i32:
    p: P
    p.x = 42
    print(p.x)
    return 0

main()
