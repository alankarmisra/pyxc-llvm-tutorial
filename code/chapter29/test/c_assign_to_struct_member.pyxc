# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x '100 200' %t
# RUN: ! grep -q "Error (Line:" %t

struct Vec2:
    x: i32
    y: i32

def main() -> i32:
    v: Vec2
    v.x = 50
    v.y = 100
    v.x = v.x + 50
    v.y = v.y + 100
    print(v.x, v.y)
    return 0

main()
