# Test that both branches returning works (both terminated paths).
# RUN: %pyxc -i %s > %t 2>&1
# RUN: grep -x 'positive' %t
# RUN: ! grep -q "Error (Line:" %t

def classify(x: i32) -> i32:
    if x > 0:
        printf("positive\n")
        return 1
    else:
        printf("non-positive\n")
        return 0

def main() -> i32:
    classify(5)
    return 0

main()
