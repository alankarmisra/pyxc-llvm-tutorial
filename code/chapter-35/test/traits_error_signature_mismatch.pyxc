# RUN: not %pyxc --emit llvm-ir -o %t.ll %s 2>&1 | FileCheck %s
# CHECK: {{[Ee]rror}}
# CHECK: does not match trait signature

trait Adder:
  def add(x: int, y: int) -> int

class Bad(Adder):
  public def add(x: int, y: float64) -> int:
    return x
