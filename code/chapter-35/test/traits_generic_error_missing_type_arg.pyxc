# RUN: not %pyxc --emit llvm-ir -o %t.ll %s 2>&1 | FileCheck %s
# CHECK: {{[Ee]rror}}
# CHECK: requires a type argument

trait Addable[T]:
  def add(x: T, y: T) -> T

class Bad(Addable):
  public def add(x: int, y: int) -> int:
    return x + y
