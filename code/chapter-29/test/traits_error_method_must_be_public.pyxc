# RUN: not %pyxc --emit llvm-ir -o %t.ll %s 2>&1 | FileCheck %s
# CHECK: {{[Ee]rror}}
# CHECK: Trait method 'add' on class 'Bad' must be public

trait Adder:
  def add(x: int, y: int) -> int

class Bad(Adder):
  private def add(x: int, y: int) -> int:
    return x + y
