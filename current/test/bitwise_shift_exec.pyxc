# RUN: %pyxc --emit exe -o %t %s
# RUN: %t 2>&1 | FileCheck %s
# CHECK: 48.000000

extern def printd(x: float64)

def main() -> int:
  var a: int = 8
  var b: int = a << 2
  var c: int = b >> 1
  printd(float64(b + c))
  return 0
